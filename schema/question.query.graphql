query getQuestion(
  $lastQuestionId: ID
  $upperLimit: Int! = 5
  $language: String!
) {
  question(
    lastQuestionId: $lastQuestionId
    upperLimit: $upperLimit
    language: $language
  ) {
    id
    question
    answers
  }
}
